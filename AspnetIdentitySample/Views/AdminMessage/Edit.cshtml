@model Examonitor.Models.AdminMessageModel

@{
    ViewBag.Title = "Boodschap Aanpassen";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Een boodschap achterlaten aan de gebruikers.</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.Id)

        <div class="col-sm-10">
            @Html.EditorFor(model => model.Message)
        </div>
         
        <div class="col-sm-10">
            @Html.EditorFor(model => model.Active)
        </div>

        <div class="btn-group">
            <input type="submit" value="Opslaan" class="btn btn-success btn-default" />
            <input type="BUTTON" value="Annuleren" class="btn btn-danger btn-default" onclick="window.location.href = '@Url.Action("Index", "MonitorBeurt")'" />
        </div>
    </div>
}
